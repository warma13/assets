# 项目级 AI 指令

## 启动必读

每次新会话开始，**第一件事**是按顺序读取以下文件:

1. `memory/WORKFLOW.md` — 工作流程规则
2. `memory/architecture.md` — 项目架构速查表

根据速查表定位目标模块，找不到再搜索实际文件。搜索后更新速查表。

## 代码修改后

每次修改代码后，检查 `memory/architecture.md` 是否需要同步更新（新增/删除/改动模块、修复待办等）。
